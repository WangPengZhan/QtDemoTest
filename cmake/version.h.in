#pragma once

// clang-format off
#define _STR(A) #A
#define STR(A) _STR(A)

#define DEMO_MAJOR ${PROJECT_VERSION_MAJOR}
#define DEMO_MINOR ${PROJECT_VERSION_MINOR}
#define DEMO_PATCH ${PROJECT_VERSION_PATCH}

#define GIT_HASH "${GIT_HASH}"
#define GIT_BRANCH "${GIT_BRANCH}"

#define DEMO_VERSION (10000 * DEMO_MAJOR + 100 * DEMO_MINOR + DEMO_PATCH)
#define DEMO_VERSION_STR STR(DEMO_MAJOR.DEMO_MINOR.DEMO_PATCH)
#define DEMO_VERSION_STR_WITH_GIT DEMO_VERSION_STR STR(GIT_HASH)

#define DEMO_BUILD_DATE "${BUILD_DATE}"
#define DEMO_BUILD_TIME "${BUILD_TIME}"

#define DEMO_BUILD_STR DEMO_BUILD_DATE " " DEMO_BUILD_TIME

// clang-format on
